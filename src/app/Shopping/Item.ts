export class Item {
    name: string;
    amount: number;
    unit: string;
    constructor(name, amount, unit) {
        this.name = name;
        this.amount = amount;
        this.unit = unit;
    }
}
